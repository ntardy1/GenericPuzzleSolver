name: continuous-integration
on:
  pull_request:
    types: [opened, reopened]
    branchs:
      - main
jobs:
  configure:
    runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
  build:
    runs-on: ubuntu-latest
      steps:
        - run: ./build.sh
